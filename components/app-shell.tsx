"use client";
import Link from "next/link";import { usePathname } from "next/navigation";import { Menu,Search,X,BookOpen } from "lucide-react";import { useCallback,useEffect,useRef,useState } from "react";import { chapters,groups } from "@/content/chapters";import { SearchDialog } from "./search-dialog";
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname(),[menu,setMenu]=useState(false),[search,setSearch]=useState(false);const menuButtonRef=useRef<HTMLButtonElement>(null),sidebarRef=useRef<HTMLElement>(null),closeButtonRef=useRef<HTMLButtonElement>(null);const closeMenu=useCallback(()=>setMenu(false),[]);useEffect(()=>{const f=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setSearch(true)}};addEventListener("keydown",f);return()=>removeEventListener("keydown",f)},[]);useEffect(()=>{if(!menu)return;const menuButton=menuButtonRef.current;closeButtonRef.current?.focus();const f=(e:KeyboardEvent)=>{if(e.key==="Escape"){e.preventDefault();closeMenu();return}if(e.key!=="Tab"||!sidebarRef.current)return;const items=[...sidebarRef.current.querySelectorAll<HTMLElement>("a[href],button:not([disabled]),summary")],first=items[0],last=items[items.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}};document.addEventListener("keydown",f);return()=>{document.removeEventListener("keydown",f);menuButton?.focus()}},[closeMenu,menu]);return <>
 <header className="topbar"><Link href="/" className="brand" aria-label="SEIF Inicio"><span className="brandmark">S</span><span><b>SEIF</b><small>Product Operating System</small></span></Link><nav className="toplinks" aria-label="Navegación principal"><Link href="/chapter/executive-summary">Assessment</Link><Link href="/chapter/operating-model">Operating Model</Link><Link href="/chapter/roadmap">Implementation</Link><Link href="/chapter/jira">Jira</Link><Link href="/chapter/north-star">Metrics</Link><Link href="/glossary">Glossary</Link></nav><button className="searchbtn" onClick={()=>setSearch(true)}><Search size={17}/><span>Buscar</span><kbd>⌘ K</kbd></button><button ref={menuButtonRef} className="menubtn" onClick={()=>setMenu(v=>!v)} aria-expanded={menu} aria-controls="chapter-navigation" aria-label={menu?"Cerrar navegación":"Abrir navegación"}>{menu?<X/>:<Menu/>}</button></header>
 <div className="appgrid"><aside ref={sidebarRef} id="chapter-navigation" className={`sidebar ${menu?"open":""}`} aria-label="Navegación de capítulos"><div className="sidehead"><span>Repositorio maestro</span><button ref={closeButtonRef} onClick={closeMenu} aria-label="Cerrar navegación"><X/></button></div>{groups.map(g=><details key={g} open><summary>{g}</summary>{chapters.filter(c=>c.group===g).map(c=><Link className={path===`/chapter/${c.slug}/`||path===`/chapter/${c.slug}`?"active":""} onClick={closeMenu} href={`/chapter/${c.slug}`} key={c.slug}>{c.title}</Link>)}</details>)}<Link onClick={closeMenu} className={path.startsWith("/glossary")?"active glossarylink":"glossarylink"} href="/glossary"><BookOpen size={16}/> Glosario</Link><footer>Assessment & Blueprint<br/><b>v0.1</b> · Actualizado ago 2026</footer></aside><main id="content">{children}</main></div>
 {menu&&<button className="scrim" onClick={closeMenu} aria-label="Cerrar navegación"/>}<SearchDialog open={search} onClose={()=>setSearch(false)}/></>}
