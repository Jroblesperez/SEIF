"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { Menu, Search, X, BookOpen } from "lucide-react"; import { useEffect,useState } from "react"; import { chapters,groups } from "@/content/chapters"; import { SearchDialog } from "./search-dialog";
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname(),[menu,setMenu]=useState(false),[search,setSearch]=useState(false);useEffect(()=>{const f=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setSearch(true)}};addEventListener("keydown",f);return()=>removeEventListener("keydown",f)},[]);return <>
 <header className="topbar"><Link href="/" className="brand" aria-label="SEIF Inicio"><span className="brandmark">S</span><span><b>SEIF</b><small>Product Operating System</small></span></Link><nav className="toplinks" aria-label="Navegación principal"><Link href="/chapter/executive-summary">Assessment</Link><Link href="/chapter/operating-model">Operating Model</Link><Link href="/chapter/roadmap">Implementation</Link><Link href="/chapter/jira">Jira</Link><Link href="/chapter/north-star">Metrics</Link><Link href="/glossary">Glossary</Link></nav><button className="searchbtn" onClick={()=>setSearch(true)}><Search size={17}/><span>Buscar</span><kbd>⌘ K</kbd></button><button className="menubtn" onClick={()=>setMenu(!menu)} aria-label="Abrir navegación"><Menu/></button></header>
 <div className="appgrid"><aside className={"sidebar "+(menu?"open":"")}><div className="sidehead"><span>Repositorio maestro</span><button onClick={()=>setMenu(false)} aria-label="Cerrar"><X/></button></div>{groups.map(g=><details key={g} open><summary>{g}</summary>{chapters.filter(c=>c.group===g).map(c=><Link className={path===`/chapter/${c.slug}/`||path===`/chapter/${c.slug}`?"active":""} onClick={()=>setMenu(false)} href={`/chapter/${c.slug}`} key={c.slug}>{c.title}</Link>)}</details>)}<Link className={path.startsWith("/glossary")?"active glossarylink":"glossarylink"} href="/glossary"><BookOpen size={16}/> Glosario</Link><footer>Assessment & Blueprint<br/><b>v0.1</b> · Actualizado ago 2026</footer></aside><main id="content">{children}</main></div>
 {menu&&<button className="scrim" onClick={()=>setMenu(false)} aria-label="Cerrar navegación"/>}<SearchDialog open={search} onClose={()=>setSearch(false)}/></>}
