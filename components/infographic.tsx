"use client";import Image from "next/image";import { useState } from "react";import { Maximize2,X } from "lucide-react";
export function Infographic({src,title,caption,relatedSection}:{src:string,title:string,caption:string,relatedSection:string}){const [open,setOpen]=useState(false);return <figure className="infographic"><button onClick={()=>setOpen(true)} aria-label={`Ampliar ${title}`}><Image src={src} alt={title} width={1400} height={788}/><Maximize2/></button><figcaption><b>{title}</b><span>{caption} · {relatedSection}</span></figcaption>{open&&<div className="lightbox" role="dialog" aria-modal="true" onClick={()=>setOpen(false)}><button aria-label="Cerrar"><X/></button><Image src={src} alt={title} width={1800} height={1013}/></div>}</figure>}
